function [ output_args ] = TomasiKanadeFactorization( D )
%

    % 
    [U,W,V] = svd(D);
    
    sqrtW3 = sqrt(W(1:3,1:3));
    M = U(:,1:3) * sqrtW;
    S = sqrtW * V(:,1:3).T;
    
    %
    L = inv( tranpose(M) * M );
    C = chol(L);
    
    
end

